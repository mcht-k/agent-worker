#!/usr/bin/env python3
"""Agent Runner — universal AI task orchestrator entry point."""

import sys
from pathlib import Path

# Add parent dir to path so runner package is importable
sys.path.insert(0, str(Path(__file__).parent))

from runner.cli import main

sys.exit(main())
